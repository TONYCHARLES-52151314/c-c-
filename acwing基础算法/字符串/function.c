#include<iostream>
#include<string.h>//关于字符串的函数

using namespace std;

int main()
{
    strlen()
    //不包含\0
    //字典序比较

    for(int i=0;str[i];i++) 遍历内容。。。//这里是string有一层遍历了


    替换为for(int i=0,len=strlen[];i<len;i++)
    
    
    strcmp()
    //字典序具有贪心的性质

    strcpy()//后面的赋给前面的
    return 0;
}

//在控制台回车，起刷新缓存的作用，终端的特性